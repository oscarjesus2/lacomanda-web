apiVersion: v1
kind: ConfigMap
metadata:
  name: sonarqube-proxy-config
data:
  default.conf: |
    map $http_x_forwarded_proto $sonarqube_forwarded_proto {
      default $http_x_forwarded_proto;
      ""      $scheme;
    }

    map $http_upgrade $sonarqube_connection_upgrade {
      default upgrade;
      ""      close;
    }

    server {
      listen 8080;
      server_name _;

      server_tokens off;
      client_max_body_size 100m;

      location = /healthz {
        access_log off;
        default_type text/plain;
        return 200 "ok\n";
      }

      location / {
        proxy_pass http://sonarqube-service.sonarqube.svc.cluster.local:9000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Proto $sonarqube_forwarded_proto;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $sonarqube_connection_upgrade;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_request_buffering off;
        proxy_redirect off;
      }
    }
---
apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: sonarqube-proxy-backendconfig
spec:
  timeoutSec: 3600
  connectionDraining:
    drainingTimeoutSec: 60
  healthCheck:
    type: HTTP
    requestPath: /healthz
    port: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sonarqube-proxy
  labels:
    app: sonarqube-proxy
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: sonarqube-proxy
  template:
    metadata:
      labels:
        app: sonarqube-proxy
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - name: http
              containerPort: 8080
          resources:
            requests:
              cpu: "10m"
              memory: "32Mi"
            limits: {}
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
              readOnly: true
      volumes:
        - name: nginx-config
          configMap:
            name: sonarqube-proxy-config
---
apiVersion: v1
kind: Service
metadata:
  name: sonarqube-proxy
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
    cloud.google.com/backend-config: '{"default": "sonarqube-proxy-backendconfig"}'
spec:
  type: ClusterIP
  selector:
    app: sonarqube-proxy
  ports:
    - name: http
      port: 80
      targetPort: http
